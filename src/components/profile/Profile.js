import React from 'react';
import { ProfileContainer } from './ProfileContainer';
export const Profile = () => {
    return (
        <ProfileContainer>
            Profile
        </ProfileContainer>
    )
}